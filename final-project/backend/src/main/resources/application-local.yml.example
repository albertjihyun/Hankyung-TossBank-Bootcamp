# 복사해서 application-local.yml로 사용 (gitignore — 03 §5)
# docker-compose.yml 기본값과 짝이 맞는 로컬 개발용 값
spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/jarvis
    username: jarvis
    password: jarvis-local
  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: # AT/RT 서명용 HS256 시크릿 — 256bit 이상 임의 문자열 (03 §5)
